# 변경 이력

번들을 새로 만들 때마다 여기에 먼저 적고, 그대로 Play Console 출시 노트에
붙여 넣는다. 기준은 **versionCode** — 정식 출시 전까지 versionName은 `1.0.0`
으로 고정이라, 빌드를 가르는 건 versionCode뿐이다.

각 항목은 "코드에서 뭘 바꿨나"가 아니라 **테스터가 화면에서 무엇을 다르게
보게 되나**로 적는다. 내부 테스트에서는 이게 곧 "무엇을 다시 봐 달라"는
요청이기도 하다. 사용자 눈에 보이지 않는 변경(테스트 추가, 리팩터링)은
적지 않는다.

---

## 5 (1.0.0) — 미출시

테스터 의견에서 나온 것 둘과, 이름이 틀려 있던 것 하나.

- **아래에서 올라오는 창을 닫기 쉬워졌다.** 위쪽 손잡이를 아래로 끌거나
  창 바깥을 눌러 닫을 수 있다. 특히 **카테고리 예산 추가** 창이 화면을 거의
  다 덮어서 닫을 곳이 없었는데, 카테고리 목록을 옆으로 넘겨 보게 바꿔 창이
  훨씬 짧아졌다. (제보: 유석)
- **색 테마가 생겼다.** 설정 → 색 테마에서 퍼플·민트·핑크·옐로우·블루 중
  고를 수 있다. 고른 색에 맞춰 배경 톤까지 함께 바뀐다. 어두운 모드에서는
  배경은 그대로고 포인트 색만 바뀐다. 기본값은 지금까지와 같은 퍼플이라,
  따로 고르지 않으면 화면은 그대로다. (제보: 예슬)
- **"백업 파일로 저장하기"를 "백업 파일 내보내기"로 고쳤다.** 누르면 카카오톡·
  Gmail이 늘어선 공유 창이 뜨는데 버튼은 "저장"이라고 해서, 저장할 곳을 찾다
  멈추는 일이 있었다. 어디에 두면 되는지 안내도 한 줄 넣었다. 복원 쪽에도
  중간에 창이 한 번 더 뜬다는 안내를 붙였다. (제보: 태경)
- **"알람"을 "알림"으로 고쳤다.** 하는 일은 정한 시각에 하루 한 번 "오늘
  지출 기록했어?"를 띄우는 것인데, 알람이라고 되어 있어 시계 알람처럼
  울리는 줄 알기 쉬웠다. 무엇이 언제 오는지 설명도 한 줄 넣었다.

## 4 (1.0.0) — 2026-08-09 비공개 테스트 출시

화면이 제대로 나오지 않던 문제 두 가지를 고쳤다. 둘 다 브라우저에서는
재현되지 않고 실제 기기에서만 나타나던 것들이다.

- **목록이 스크롤되지 않고 아래 탭 막대가 보이지 않던 문제.** 안드로이드
  시스템 WebView가 오래된 기기에서 앱 전체 높이 계산이 무너져, 내용이
  화면 아래로 잘려 나가고 스크롤이 죽어 있었다. 끌면 한 번 울렁이고 멈췄다.
- **알림 문구(토스트) 배경이 사라지던 문제.** 같은 원인으로 배경과 그림자가
  통째로 빠져 글자만 떠 있었다.
- **폴더블을 펼치면 앱이 화면 가운데 작은 카드처럼 뜨던 문제.** 개발용
  화면 틀이 실제 기기까지 따라 나가고 있었다. 이제 펼친 화면에서는 배경이
  화면을 채우고 내용이 가운데로 정렬된다. (versionCode 3에서 고쳤으나 그
  빌드는 올리지 않아, 이번 빌드에 함께 들어간다.)

## 3 (1.0.0) — 올리지 않음

폴더블 대응만 담아 만들었으나 업로드 전에 위 스크롤 문제를 발견해,
4에 합쳐 올리기로 했다. versionCode는 중간이 비어도 되므로 그대로 건너뛴다.

## 2 (1.0.0)

- **반복 지출이 탭 한 번으로 기록하는 템플릿으로 바뀌었다.** 매주/매월/매년,
  시작일, 종료일이 없어졌다. 정해진 날짜에 자동으로 기록되는 대신, 이름과
  금액·카테고리·결제수단을 저장해 두고 필요할 때 불러 쓴다.
- **저장해 둔 지출이 입력 화면 금액 시트에 칩으로 뜬다.** 누르면 금액부터
  결제수단까지 한 번에 채워지고, 저장은 평소처럼 "추가!"로 한다.
- 반복 지출은 **10개까지** 만들 수 있고, 각각 **표시/숨김**을 골라 입력
  화면에 띄울 것만 남길 수 있다.
- **금액 지우기 (×) 버튼**이 모든 키패드에 생겼다.
- 금액 자릿수가 늘어나도 글자가 잘리거나 레이아웃이 밀리지 않는다.
- 년·월을 눌러 원하는 달로 바로 이동할 수 있다.
- 화면이 짧은 기기에서 시트 내용이 눌려 찌그러지던 문제를 고쳤다.

## 1 (1.0.0)

첫 내부 테스트 빌드.
