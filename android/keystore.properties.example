# Copy to android/keystore.properties and fill in.
# That copy is gitignored — the real passwords must never be committed.
#
# storeFile can be an absolute path; keeping the .jks outside the repo is the
# safest place for it.
storeFile=/Users/seungmi/on-expense-upload.jks
storePassword=여기에_키스토어_비밀번호
keyAlias=upload
keyPassword=여기에_키_비밀번호
