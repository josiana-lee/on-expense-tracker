# Dependencies
node_modules/
.pnpm-store/

# Build output
dist/
dist-ssr/
dev-dist/
*.local

*.tsbuildinfo

# Editor / OS
.claude/settings.local.json
.DS_Store
.idea/
*.iml
*.log
.vscode/*
!.vscode/extensions.json

# Env
.env
.env.*
!.env.example

# --- Android wrapper (Capacitor/TWA, added when we package for Play Store) ---
android/app/build/
android/build/
android/.gradle/
android/local.properties
captures/
*.jks
*.keystore
*.aab
*.apk
*.hprof
google-services.json

# Upload-key password file. The keystore itself is already covered by *.jks
# above; this holds the passwords that open it and must never be committed.
android/keystore.properties
